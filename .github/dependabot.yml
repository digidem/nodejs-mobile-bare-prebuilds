version: 2
updates:
  - package-ecosystem: github-actions
    # Walks the whole .github/ tree, so composite actions under
    # .github/actions/*/action.yml are covered alongside workflows.
    directory: /
    schedule:
      interval: weekly
